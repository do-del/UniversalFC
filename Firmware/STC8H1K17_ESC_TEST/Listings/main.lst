C51 COMPILER V9.60.0.0   MAIN                                                              01/12/2023 17:00:20 PAGE 1   


C51 COMPILER V9.60.0.0, COMPILATION OF MODULE MAIN
OBJECT MODULE PLACED IN .\Objects\main.obj
COMPILER INVOKED BY: C:\Keil_c51\C51\BIN\C51.EXE main.c OPTIMIZE(8,SPEED) BROWSE DEBUG OBJECTEXTEND PRINT(.\Listings\mai
                    -n.lst) TABS(2) OBJECT(.\Objects\main.obj)

line level    source

   1          #include "stc8h.h"
   2          #include <intrins.h>
   3          
   4          /*
   5           * PWM1接P1.0，配置推挽输出和PWM输出，控制电机U相高位MOS管选通信号，高电平导通，低电平截止
   6           * PWM1_L接P1.1，配置推挽输出，控制电机U相低位MOS管选通信号，高电平导通，低电平截止
   7           * PWM2接P1.2，配置推挽输出和PWM输出，控制电机V相高位MOS管选通信号，高电平导通，低电平截止
   8           * PWM2_L接P1.3，配置推挽输出，控制电机V相低位MOS管选通信号，高电平导通，低电平截止
   9           * PWM3接P1.4，配置推挽输出和PWM输出，控制电机W相高位MOS管选通信号，高电平导通，低电平截止
  10           * PWM3_L接P1.5，配置推挽输出，控制电机W相低位MOS管选通信号，高电平导通，低电平截止
  11           * ADC8（P0.0），配置高阻输入，采样电机U相感应电动势，用于过零检测比较器正输入
  12           * ADC9（P0.1），配置高阻输入，采样电机V相感应电动势
  13           * ADC10（P0.2），配置高阻输入，采样电机W相感应电动势
  14           * CMP-（P3.6），配置为高阻输入，接过零检测电路中点
  15          */
  16          
  17          typedef unsigned char u8;
  18          typedef unsigned int u16;
  19          typedef unsigned long u32;
  20          
  21          #define PWM1    P10
  22          #define PWM1_L  P11
  23          #define PWM2    P12
  24          #define PWM2_L  P13
  25          #define PWM3    P14
  26          #define PWM3_L  P15
  27          
  28          void Port_Init(void); //芯片复位后引脚初始化
  29          void PWMA_Config(void); //PWMA配置函数
  30          void ADC_Config(void);
  31          void CMP_Config(void);
  32          void Timer0_Config(void);
  33          void Timer3_Config(void);
  34          void Timer4_Config(void);
  35          
  36          void main(void)
  37          {
  38   1        P_SW2 |= 0x80; //使能XFR
  39   1        
  40   1        Port_Init();
  41   1        PWMA_Config();
  42   1        ADC_Config();
  43   1        CMP_Config();
  44   1        Timer0_Config();
  45   1        Timer3_Config();
  46   1        Timer4_Config();
  47   1      }
  48          
  49          void Port_Init(void)
  50          {
  51   1        P0M0 = 0x00;
  52   1        P0M1 = 0x00; //P0端口初始化为双向口
  53   1        P1M0 = 0x00;
  54   1        P1M1 = 0x00; //P1端口初始化为双向口
C51 COMPILER V9.60.0.0   MAIN                                                              01/12/2023 17:00:20 PAGE 2   

  55   1        P2M0 = 0x00;
  56   1        P2M1 = 0x00; //P2端口初始化为双向口
  57   1        P3M0 = 0x00;
  58   1        P3M1 = 0x00; //P3端口初始化为双向口
  59   1        P5M0 = 0x00;
  60   1        P5M0 = 0x00; //P5端口初始化为双向口
  61   1      }
  62          
  63          void PWMA_Config(void)
  64          {
  65   1        //先将MOS管选通信号拉低，防止误导通
  66   1        PWM1 = 0;
  67   1        PWM1_L = 0;
  68   1        PWM2 = 0;
  69   1        PWM2_L = 0;
  70   1        PWM3 = 0;
  71   1        PWM3_L = 0;
  72   1        
  73   1        //配置选通引脚为推挽输出
  74   1        P1M0 |= 0x3f; //0x3f = 0B0011 1111，bit0~bit5置1
  75   1        P1M1 &= ~0x3f; //~0x3f = 0B1100 0000,bit0~bit5置0
  76   1        
  77   1        PWMA_PSCR = 3;  //PWMA_PSCR为PWMA的16位预分频器寄存器，可以16位数据读写，f_ck_int = f_ck_psc/(PSCR[15:0]+1
             -)
  78   1        PWMA_DTR = 24;  //PWMA_DTR位PWMA的死区寄存器，设置死区持续时间，本程序死区互补功能未开
  79   1        
  80   1        PWMA_ARR = 255; //PWMA_ARR为PWMA的16位自动重载寄存器
  81   1        PWMA_CCER1 = 0; //捕获/比较使能寄存器1，配置极性及输出使能
  82   1        PWMA_CCER2 = 0; //捕获/比较使能寄存器2，配置极性及输出使能
  83   1        PWMA_SR1 = 0;   //状态寄存器1，中断标记
  84   1        PWMA_SR2 = 0;   //状态寄存器2，重复捕获标记
  85   1        PWMA_ENO = 0;   //输出使能寄存器
  86   1        PWMA_PS = 0;    //功能脚切换
  87   1        PWMA_IER = 0;   //中断使能寄存器
  88   1        
  89   1        PWMA_CCMR1 = 0x68;  //捕获/比较模式寄存器1，0x68 = 0b0110 1000配置为输出，开启预装载，PWM模式1
  90   1        PWMA_CCR1 = 0;      //捕获/比较寄存器，16位，当前比较值
  91   1        PWMA_CCER1 |= 0x05; //0x05 = 0b0000 0101
  92   1        PWMA_PS |= 0;
  93   1      }
  94          
  95          void ADC_Config(void);
  96          void CMP_Config(void);
  97          void Timer0_Config(void);
  98          void Timer3_Config(void);
  99          void Timer4_Config(void);


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    137    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =   ----    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
